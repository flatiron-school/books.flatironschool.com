<!DOCTYPE html>
<html dir="ltr">
  <head>
    <title>Flatiron Library</title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
    <%= analytics_init if Rails.env.production? %>
    <link href='http://fonts.googleapis.com/css?family=Kite+One' rel='stylesheet' type='text/css'>
    <link href="/assets/logos/flatiron-favicon.ico" rel="icon" type="image/vnd.microsoft.icon"/>
  </head>
  <body tabindex="1" class="<%= "#{params[:controller]} #{params[:action]}" %>">
    <div class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><img src= "<%= "#{root_url}" %>assets/logos/flatiron.svg"/></a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% if current_user %>
              <li><a href="/">Library</a>
            <% end %>
            <% if current_user && current_user.admin %>
              <li><%= link_to "Admin", admin_path %></li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if current_user %>
              <li><%= link_to "Sign out", signout_path %></li>
            <% else %>
              <li><a href="/auth/github">Sign in with GitHub</a></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div><!--/.container-fluid -->
    </div><!-- nav bar -->
    <div class="container-fluid">
      <%= yield %>
    </div>
  </body>
</html>

